---
slug: codex-cli-usage-guide
title: 'Codex 사용법: 코덱스 CLI 설치 다음 첫 세션에서 정할 네 가지'
excerpt: >-
  코덱스 CLI의 첫 세션은 어느 폴더에서 열지, 어디까지 허용할지, 어떤 규칙 파일을 읽게 할지, 무엇부터 맡길지를 정하는 단계입니다. 처음
  치는 명령, 승인 범위를 바꾸는 방법, AGENTS.md를 두는 순서를 공식 문서로 확인해 정리했습니다.
category: AI_TECH
tags:
  - AI
  - 자동화
  - 바이브코딩
publishedAt: '2026-09-15T06:20:00.000Z'
highlights:
  - 설치한 폴더가 아니라 고칠 저장소 폴더로 이동한 뒤에 codex를 칩니다.
  - '첫 세션은 읽기만 시키고, 파일을 고치게 하기 전에 Git 체크포인트를 먼저 만듭니다.'
quiz:
  - question: 코덱스 CLI 세션에서 /init 명령을 실행하면 무엇이 만들어질까요?
    options:
      - AGENTS.md 초안 파일. 저장소 규칙을 적어 두면 세션을 열 때마다 읽습니다
      - config.toml 설정 파일. 기본 모델과 추론 깊이를 저장합니다
      - .gitignore 파일. 코덱스가 건드리지 않을 경로를 적습니다
      - 세션 기록 파일. 지난 대화를 다시 불러올 때 씁니다
    correctIndex: 0
    explanation: >-
      코덱스 공식 문서는 /init을 코덱스에 줄 지시를 담은 AGENTS.md 파일을 만드는 명령이라고 안내합니다. 만들어지는 것은
      뼈대뿐이라 내용은 사람이 채워야 합니다. 기본 모델과 추론 깊이를 저장하는 곳은 config.toml로 따로 있습니다.
metaTitle: 'Codex 사용법: 코덱스 CLI 설치 다음 첫 세션에서 정할 네 가지'
metaDescription: >-
  코덱스 CLI의 첫 세션은 어느 폴더에서 열지, 어디까지 허용할지, 어떤 규칙 파일을 읽게 할지, 무엇부터 맡길지를 정하는 단계입니다. 처음
  치는 명령과 승인 범위 조절, AGENTS.md 배치 순서를 2026년 9월 공식 문서로 확인해 정리했습니다.
ogDescription: >-
  설치 다음에 무엇을 칠지 막히는 분들을 위해 작업 폴더 지정, 권한 조절, AGENTS.md 배치, 처음 맡기기 좋은 일을 순서대로
  정리했습니다.
ogImage: /og/codex-cli-usage-guide.png
summary3:
  - 코덱스 CLI는 codex를 친 폴더를 작업 범위로 잡으므로 설치 폴더가 아니라 고칠 저장소로 이동한 뒤에 실행합니다.
  - >-
    코덱스의 권한은 쓰기 범위를 정하는 샌드박스와 언제 물어볼지 정하는 승인 정책 두 가지로 정해지고 세션 도중에는
    /permissions에서 바꿉니다.
  - 저장소 루트에 둔 AGENTS.md는 세션을 열 때 코덱스가 한 번 읽으므로 파일을 고쳤으면 세션을 다시 열어야 반영됩니다.
---

코덱스 CLI(Codex CLI)의 첫 세션은 어느 폴더에서 열지, 어디까지 허용할지, 어떤 규칙 파일을 읽게 할지, 무엇부터 맡길지를 정하는 단계입니다. 설치 명령과 달리 이 네 가지는 한 줄로 끝나지 않고, 어느 것부터 정해야 하는지도 화면에 적혀 있지 않습니다.

설치는 짧게 끝납니다. 맥과 리눅스는 설치 스크립트 한 줄, 윈도우는 PowerShell 한 줄이면 되고 npm이나 Homebrew로 받는 방법도 따로 있습니다. 준이아빠블로그에도 [윈도우 설치 가이드](/insights/codex-cli-windows-easy-setup-guide) (윈도우 설치 순서 바로가기)와 [맥 설치 가이드](/insights/codex-cli-mac-easy-setup-guide) (맥 설치 순서 바로가기)를 단계별로 적어 두었습니다.

막히는 곳은 그다음입니다. `codex`를 치면 빈 입력창에 커서만 깜빡이는데, 아무 폴더에서나 열고 승인 창을 계속 넘기다 보면 고칠 생각이 없던 파일까지 바뀌어 있기도 합니다. 아래에는 2026년 9월 15일에 코덱스 공식 문서와 실제로 설치된 0.154.0 버전에서 확인한 것만 담았습니다.

처음 온 사람에게 일할 건물을 정해 주고 그 안의 방 열쇠를 골라 건네듯, 코덱스에도 쓸 폴더를 정해 주고 권한을 골라 주는 순서로 시작합니다. 실행하면 뜨는 화면은 이렇습니다.

![코덱스 CLI를 실행한 터미널 화면. 위쪽 상자에 지금 쓰는 모델 이름과 작업 폴더 경로가 적혀 있고 그 아래에 슬래시로 시작하는 명령 다섯 가지가 한 줄씩 적혀 있다](/images/insights/codex-cli-usage-guide/02-first-run-commands.png)

코덱스 도움말 페이지에 실린 실행 화면입니다. 상자 안에 지금 쓰는 모델 이름과 작업 폴더가 먼저 나오고, 그 아래에 명령 다섯 가지가 한 줄씩 이어집니다. 이 화면이 첫 세션에서 정할 것을 거의 다 보여 줍니다.

## 시작 폴더가 코덱스의 작업 범위를 정합니다

코덱스는 명령을 친 폴더를 작업 루트로 잡습니다. 공식 빠른 시작 안내도 프로젝트 폴더를 연 다음 `codex`를 실행하라고 적고 있습니다. 홈 폴더에서 그냥 열면 코덱스가 저장소 구조를 모르는 상태로 시작합니다.

그래서 순서가 하나 늘어납니다. 고칠 프로젝트로 먼저 이동한 뒤에 실행합니다.

```bash
cd ~/code/my-app
codex
```

매번 폴더를 옮겨 다니기가 번거로우면 실행할 때 폴더를 지정하는 방법도 있습니다. `-C` 또는 `--cd` 옵션에 경로를 적으면 그 폴더가 기준이 됩니다.

```bash
codex -C ~/code/my-app
```

코덱스가 지금 어느 폴더에서 어떤 권한으로 돌아가고 있는지 모르겠으면 세션 안에서 `/status`를 칩니다. 현재 세션 설정을 그대로 보여 주는 명령입니다.

프로젝트 폴더 바깥의 파일까지 고쳐야 하는 경우도 있습니다. 이때 권한을 통째로 여는 대신 `--add-dir`로 필요한 폴더만 추가합니다. 공식 안내도 쓰기 범위를 넓혀야 하면 `danger-full-access`로 올리기보다 `--add-dir`를 쓰라고 권합니다.

```bash
codex --add-dir ~/code/shared-assets
```

지금 저장소를 그대로 두고 시험해 보고 싶으면 `--worktree` 옵션으로 새 Git 워크트리를 만들어 그 안에서 세션을 돌리는 방법도 있습니다.

## 첫 화면에 뜨는 명령 다섯 가지

실행 직후 화면에 나오는 다섯 가지가 첫 세션의 설정 메뉴에 해당합니다. 각각 하는 일이 달라서 이름만 보면 헷갈리는데, 한 번씩 열어 보면 그다음부터는 금방 익숙해집니다.

| 명령 | 하는 일 | 언제 쓰는지 |
| --- | --- | --- |
| `/init` | 코덱스에 줄 지시를 담은 AGENTS.md 파일을 만듭니다 | 저장소에 처음 들어갔을 때 |
| `/status` | 현재 세션 설정을 보여 줍니다 | 작업 루트와 권한이 헷갈릴 때 |
| `/permissions` | 코덱스에 무엇까지 허용할지 정합니다 | 읽기에서 쓰기로 넘어갈 때 |
| `/model` | 쓸 모델과 추론 깊이를 고릅니다 | 어려운 작업을 맡기기 전에 |
| `/review` | 바뀐 내용을 검토해 문제를 찾습니다 | 커밋하기 전에 |

모델과 추론 깊이의 차이는 [맥 설치 가이드](/insights/codex-cli-mac-easy-setup-guide) (모델과 추론 깊이 표 바로가기)에 표로 정리해 두었으므로 여기서는 나머지 넷만 다룹니다.

입력창에서 쓰는 단축키도 몇 가지 있습니다. `@`를 치면 프로젝트 안의 파일을 찾아 경로를 그대로 넣어 주고, 줄 맨 앞에 `!`를 붙이면 지금 설정된 권한 범위 안에서 셸 명령이 실행됩니다. 코덱스가 일하는 도중에 Tab을 누르면 다음에 시킬 내용을 미리 적어 둘 수 있고, Enter를 누르면 코덱스가 하던 작업에 그 지시를 바로 반영합니다. 끝낼 때는 Ctrl+C나 `/exit`입니다.

## 코덱스에 처음 시킬 요청 한 줄

공식 빠른 시작 안내는 첫 작업의 예로 `Tell me about this project`, 즉 이 프로젝트를 설명해 달라는 요청을 보여 줍니다. 한국어로 그대로 쳐도 됩니다.

```text
이 저장소가 무슨 일을 하는 프로젝트인지, 주요 폴더가 어떻게 나뉘어 있는지 설명해 줘.
```

읽기부터 시키는 이유는 되돌릴 것이 없어서입니다. 파일이 바뀌지 않으니 결과가 마음에 들지 않아도 그냥 다시 물어보면 됩니다. 그러는 동안 코덱스가 어떤 파일을 먼저 열고 무엇을 근거로 답하는지 화면에 그대로 나옵니다.

![코덱스 CLI 세션에서 이 코드베이스를 설명해 달라고 입력한 화면. 그 아래에 코덱스가 저장소를 어떻게 훑을지 적은 계획이 나오고, 확인할 항목 네 개가 체크박스 목록으로 이어져 있다](/images/insights/codex-cli-usage-guide/01-first-prompt.png)

코덱스 공식 저장소 README에 실린 실행 화면입니다. 요청 한 줄 아래로 코덱스가 먼저 무엇을 확인할지 적은 계획이 나오고, 그 계획을 항목으로 나눈 목록이 이어집니다. 이 화면이 올라온 당시의 값이라 지금 설치되는 것과는 모델 이름도 버전도 다릅니다.

여기서 확인할 것은 두 가지입니다. 코덱스가 설명한 폴더 구조가 실제와 맞는지, 그리고 답이 이 저장소를 보고 쓴 내용인지 일반론인지입니다. 둘 다 맞으면 같은 세션에서 바로 다음 일을 맡겨도 됩니다.

파일을 고치는 요청으로 넘어가기 전에 공식 빠른 시작 안내가 권하는 준비가 하나 있습니다. 작업 전후로 Git 체크포인트를 만들어 두는 것입니다. 커밋을 하나 찍어 두면 결과가 엉뚱하게 나왔을 때 되돌릴 기준이 생깁니다.

```bash
git add -A && git commit -m "코덱스에 맡기기 전 상태"
```

## /permissions로 권한 폭 정하기

코덱스의 권한은 두 가지로 나뉩니다. 쓰기를 어디까지 허용할지 정하는 샌드박스와 언제 사람에게 물어볼지 정하는 승인 정책입니다. 이 두 가지가 맞물려 지금 세션에서 코덱스가 할 수 있는 일의 범위를 정합니다.

명령줄에서 지정하는 값은 아래와 같습니다. 0.154.0 버전의 도움말에서 확인한 목록입니다.

| 옵션 | 고를 수 있는 값 |
| --- | --- |
| `--sandbox` | `read-only`, `workspace-write`, `danger-full-access` |
| `--ask-for-approval` | `on-request`, `never` |

값 자체보다 중요한 것은 조합입니다. 하려는 일에 따라 조합은 아래 세 가지로 나뉩니다.

| 하려는 일 | 샌드박스 | 승인 정책 |
| --- | --- | --- |
| 코드를 읽고 설명만 받기 | `read-only` | `on-request` |
| 프로젝트 안에서 고치게 하기 | `workspace-write` | `on-request` |
| 다른 일을 하는 동안 맡겨 두기 | `workspace-write` | `never` |

공식 문서가 대화형 작업의 기본으로 안내하는 조합은 가운데 줄입니다. 작업 루트 안에서는 코덱스가 파일을 마음대로 고치되, 그 바깥으로 나가거나 네트워크가 필요하면 그때 물어보는 방식입니다.

```bash
codex --sandbox workspace-write --ask-for-approval on-request
```

세션을 켠 뒤에 바꾸고 싶으면 `/permissions`를 칩니다. 공식 레퍼런스는 이 명령을 세션 도중에 승인 범위를 넓히거나 좁히는 용도로 설명하면서, Read Only와 Auto 사이를 오가는 예를 듭니다. 항목 이름은 버전에 따라 조금씩 다르므로 화면에 뜬 설명을 읽고 고릅니다.

방 열쇠를 하나씩 건네는 것과 건물 출입증을 통째로 넘기는 것이 다르듯, 범위를 넓히는 방법에도 단계가 있습니다. 따로 격리한 가상 머신 안이 아니면 `--dangerously-bypass-approvals-and-sandbox`를 쓰지 말라고 공식 레퍼런스가 분명히 적어 두었습니다. 코덱스가 고칠 수 있는 범위가 좁아서 생긴 문제라면 앞에서 본 `--add-dir`가 먼저입니다.

코덱스와 클로드 코드의 안전장치가 서로 다른 방식으로 짜여 있다는 점은 [코덱스 CLI와 클로드 코드 비교](/insights/codex-cli-vs-claude-code) (두 도구의 안전장치 비교 바로가기)에서 직접 돌려 보고 정리했습니다.

## AGENTS.md로 같은 설명 반복 줄이기

같은 설명을 세션마다 다시 적고 있다면 규칙 파일을 만들 때가 된 것입니다. 현관에 붙여 둔 쪽지를 새로 온 사람이 먼저 읽듯, 코덱스도 일을 시작하기 전에 `AGENTS.md`를 읽습니다.

초안은 `/init`이 만들어 줍니다. 다만 만들어지는 것은 뼈대뿐이라 내용은 사람이 채웁니다. 채워 두면 좋은 내용은 세 가지입니다.

- **빌드와 테스트 명령**: 실제로 돌아가는 명령을 그대로 적습니다. 코덱스가 결과를 스스로 확인할 때 씁니다.
- **쓰지 않기로 한 것**: 이미 걷어낸 라이브러리나 더는 쓰지 않는 패턴을 적어 두면 되살아나지 않습니다.
- **파일 배치 규칙**: 새 파일을 어디에 만들지 정해 두면 엉뚱한 폴더에 생기는 일이 줄어듭니다.

이 파일은 여러 곳에 둘 수 있어서 읽는 차례도 정해져 있습니다. 공식 안내에 적힌 차례는 이렇습니다.

1. **전역 파일**: 코덱스 홈 폴더의 `~/.codex/AGENTS.md`를 먼저 읽습니다. 같은 폴더에 `AGENTS.override.md`가 있으면 그쪽이 우선합니다.
2. **프로젝트 파일**: 프로젝트 루트에서 지금 폴더까지 내려오며 폴더마다 최대 한 개를 읽습니다.
3. **합치는 방향**: 루트부터 차례로 이어 붙이므로 현재 폴더에 가까운 파일이 뒤에 놓여 앞의 지침을 덮습니다.

주의할 점이 두 가지 있습니다. 하나는 크기입니다. 모아 읽은 파일의 합계가 32KiB에 닿으면 그 뒤 파일은 더 싣지 않으므로, 길어지면 하위 폴더로 나눠 두는 편이 안전합니다. 다른 하나는 반영 시점입니다. 코덱스가 이 파일을 세션 시작 때 한 번 읽으므로, 내용을 고쳤으면 세션을 다시 열어야 바뀐 규칙이 적용됩니다.

클로드 코드를 써 오신 분이라면 `CLAUDE.md`와 하는 일이 같습니다. 두 파일의 작성 기준은 [CLAUDE.md 설정 공식 문서 정리](/insights/claude-md-setup-official-docs) (규칙 파일 작성 기준 바로가기)에 적어 두었습니다.

## 처음 맡기기 좋은 일 네 가지

첫 며칠은 결과를 눈으로 확인할 수 있고 되돌리기도 쉬운 일부터 고릅니다. 아래 넷이 그 조건에 맞습니다.

- **저장소 설명 받기**: 파일을 바꾸지 않아 되돌릴 것이 없고, 코덱스가 이 프로젝트를 제대로 읽는지 확인하는 데도 씁니다.
- **문서 다듬기**: README나 설정 안내처럼 틀려도 코드가 멈추지 않는 파일부터 맡깁니다. 결과를 읽어 보면 바로 판단이 섭니다.
- **테스트 하나 추가**: 이미 돌아가는 기능에 테스트를 붙이는 일은 범위가 분명하고, 한 번 돌려 보면 맞는지 바로 알 수 있습니다.
- **커밋 전 검토**: `/review`를 치면 검토 대상을 고르는 화면이 뜹니다. 아직 커밋하지 않은 변경, 특정 커밋, 기준 브랜치와의 차이, 직접 적은 검토 기준 가운데 하나를 고릅니다.

마지막 항목은 특히 부담이 적습니다. 공식 안내에 따르면 검토는 작업 루트를 고치지 않고 문제만 골라 알려 주는 방식이라, 권한을 올리지 않아도 결과가 나옵니다.

```bash
codex review --uncommitted
```

## 처음부터 맡기지 않는 편이 나은 일

반대로 미룰 일도 정해 두면 초반에 헛걸음이 줄어듭니다. 아래 네 가지는 익숙해진 다음으로 미루시기를 권합니다.

- **커밋하지 않은 변경이 쌓인 폴더에서 시작하기**: 되돌릴 기준이 없으면 코덱스가 고친 부분과 내가 고치던 부분이 뒤섞입니다. 먼저 커밋하고 시작합니다.
- **폴더 바깥까지 고치려고 권한을 통째로 열기**: 필요한 폴더만 `--add-dir`로 더하면 잘못되더라도 번지는 범위가 좁습니다.
- **결과가 맞는지 판단할 방법이 없는 일**: 돌려 볼 수도 읽어 볼 수도 없는 코드를 맡기면 틀린 코드가 그대로 쌓입니다. 확인 절차가 있는 작업부터 고르는 쪽이 안전합니다.
- **규칙 파일을 고치자마자 같은 세션에서 확인하기**: 앞에서 본 대로 코덱스가 세션 시작 때 한 번 읽으므로 바뀐 내용이 반영되지 않습니다.

인터넷에서 자료를 받아야 하는 작업도 처음에는 빼 두는 쪽이 낫습니다. `workspace-write`에서 네트워크는 기본으로 닫혀 있어 설정에서 따로 열어야 합니다. 여는 순서는 [코덱스 CLI 사용법](/insights/codex-cli-practical-guide) (네트워크 설정을 여는 순서 바로가기)에서 실행 화면과 함께 다뤘습니다.

## 자주 묻는 질문

### 설치만 하고 아직 아무것도 못 했는데 무엇부터 치면 되나요?

고칠 프로젝트 폴더로 이동한 뒤 `codex`를 실행하고, 이 저장소가 무슨 프로젝트인지 한국어로 물어봅니다. 파일이 바뀌지 않는 요청이라 잘못되어도 되돌릴 것이 없습니다. 답이 실제 폴더 구조와 맞으면 그다음부터 문서 수정처럼 작은 일을 맡깁니다.

### 승인 창이 계속 떠서 번거로운데 꺼도 되나요?

프로젝트 안에서만 고치는 작업이라면 `--sandbox workspace-write --ask-for-approval on-request` 조합으로 시작합니다. 그 안에서는 묻지 않고, 바깥으로 나가거나 네트워크가 필요할 때만 물어봅니다. 승인을 아예 끄는 `never`는 Git으로 되돌릴 준비가 되어 있을 때만 쓰시는 편이 안전합니다.

### AGENTS.md를 만들었는데 코덱스가 따르지 않으면 어디를 보면 되나요?

먼저 세션을 다시 열어 봅니다. 코덱스가 이 파일을 세션 시작 때 한 번 읽으므로 열어 둔 채로 고치면 반영되지 않습니다. 그래도 달라지지 않으면 상위 폴더나 코덱스 홈 폴더에 `AGENTS.override.md`가 있는지 확인합니다. 이 파일이 있으면 같은 범위의 `AGENTS.md`를 대신합니다.

## 3줄 요약

1. **코덱스 CLI는 `codex`를 친 폴더를 작업 범위로 잡습니다.** 설치한 폴더가 아니라 고칠 저장소로 이동한 뒤에 실행하고, 폴더가 더 필요하면 `--add-dir`로 추가합니다.
2. **권한은 쓰기 범위를 정하는 샌드박스와 언제 물어볼지 정하는 승인 정책 두 가지로 정해집니다.** 세션 도중에는 `/permissions`로 바꿉니다.
3. **저장소 루트에 둔 `AGENTS.md`는 세션을 열 때 코덱스가 한 번 읽습니다.** 파일을 고쳤으면 세션을 다시 열어야 바뀐 규칙이 적용되고, 합친 크기가 32KiB에 닿으면 그 뒤 파일은 싣지 않습니다.

## Sources

- [Codex CLI 공식 문서](https://learn.chatgpt.com/docs/codex/cli)
- [Codex CLI 명령과 옵션 레퍼런스](https://developers.openai.com/codex/cli/reference)
- [AGENTS.md 공식 가이드](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [Codex 권한 공식 안내](https://developers.openai.com/codex/permissions)
- [openai/codex 공식 저장소](https://github.com/openai/codex)
