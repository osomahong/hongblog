# 블로그 작업 규칙

기존 콘텐츠, 디자인, 배포 규칙은 CLAUDE.md를 함께 읽는다. 사용자 승인과 기존 변경을 보존한다.

## 모든 변경의 유지보수 확인

- 관련 본문, 요약, 퀴즈, FAQ, 메타, JSON-LD의 정의와 수치를 함께 대조한다. 통계에는 기간/분모/분류 규칙을 남긴다. referral, 브랜드 언급, 직접 URL 인용을 구분한다.
- 기술 내용의 실질 개정은 공식 출처와 적용 환경을 확인하고 실제 확인 날짜를 남긴다. 빌드 날짜만으로 검증일을 갱신하지 않는다. 기존 파일의 updatedAt 및 GA4 Edu reviewedAt를 사용한다.
- 작성자는 AUTHOR_PERSON_LD와 AUTHOR_LABEL을 재사용한다. 수강 개념 수를 학점(numberOfCredits)으로 표시하지 않는다.
- 구독/요약/실습을 수정하면 비구독/구독 상태를 모두 확인하고 공개 설명이 서버 HTML에 남는지와 hasPart.cssSelector 일치를 확인한다. 계정이 없으면 미검증 상태를 명시한다.
- 콘텐츠 추가/이동 때 llms.txt의 선별 링크를 점검한다. 전체 목록/수치라고 표시하지 않으며 허브/사이트맵과 역할을 구분한다.
- npm run build는 prebuild 링크/제목과 postbuild의 check:maintenance를 실행한다. 자동 검사는 의미적 정확성이나 실제 구독 계정을 대신하지 않는다.
- 작업 완료 기록에 적용한 유지보수 항목, 실행 명령/결과, 미확인 항목과 이유를 남긴다. 검사 실패를 숨기거나 자동으로 예외 목록에 넣지 않는다.
