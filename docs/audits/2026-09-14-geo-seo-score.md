# 전체 콘텐츠 GEO/SEO 점수 점검 (2026-09-14)

362편(insights 220, classes 131, courses 11) 전수 측정과 보강 기록이다.

## 측정 도구

`npm run check:geo` (`scripts/geo-seo-score.ts`). 규칙 원본은
`.claude/skills/inspect-content/references/{seo,aeo,geo}-rules.md`이고, 그 가운데
결정론으로 판정 가능한 층을 전수 실행한다.

종합 점수 = SEO × 0.35 + AEO × 0.30 + GEO × 0.35. 타입별로 적용되지 않는 규칙은
N/A로 빼고 적용 가중치 합으로 나눠 100점으로 환산한다.

### 원본 명세에서 바꾼 기준

| 규칙 | 원본 | 이 저장소 | 이유 |
|---|---|---|---|
| R-SEO-01 | metaTitle 30~60자 | 표시 폭 18~36 | `check-titles.ts`가 표시 폭 36을 SOFT 상한으로 쓴다. 글자 수로 재면 두 검사가 반대 방향을 가리킨다 |
| R-SEO-06 | ogTitle + ogDescription | ogDescription만 | `check-titles.ts`가 metaTitle의 축약판 ogTitle을 SOFT로 지적한다. 공유용 문장이 따로 없으면 없는 편이 맞다 |
| R-AEO-02, R-AEO-03 | classes 면제 | classes도 적용 | 클래스도 FAQPage를 발행한다. 면제하면 실제 구멍이 가려진다 |
| R-AEO-05 | 요약 헤딩 + summary3 둘 다 | 둘 중 하나 | summary3는 화면에서 잠겨도 HTML에는 항상 들어간다(`src/lib/summary-gate.ts`). 답변 엔진 기준으로는 같은 값이다 |
| R-AEO-07 | 모든 약어 | 본문에 3회 이상 쓰인 약어, 일반 약어 제외 | AI, PDF, PNG까지 정의를 요구하면 오탐이 전체의 절반을 넘는다. 내부 링크가 걸린 용어는 정의된 것으로 센다 |

`R-GEO-01`(출처 인용)과 `R-GEO-02`(통계 밀도)는 보강 작업 목록에서 제외했다. 없는 근거와
수치를 지어내는 것이 점수를 올리는 가장 빠른 길이기 때문이다. 측정은 하되 조치는
주제별로 따로 판단한다.

## 측정 결과 (보강 전)

| 구분 | 종합 | SEO | AEO | GEO |
|---|---|---|---|---|
| insights (220) | 86.2 | 94.2 | 75.4 | 87.5 |
| classes (131) | 81.8 | 85.3 | 77.8 | 81.7 |
| courses (11) | 39.5 | 83.6 | 19.4 | 12.7 |
| 전체 (362) | 83.2 | 90.6 | 74.6 | 83.1 |

등급 분포: A 255편, B 94편, C 9편, D 4편.

## 찾은 결손

### 1. 코스 페이지의 세팅 결손 세 가지

하위 11편이 전부 코스였다. 본문 문제가 아니라 페이지 쪽 결손이었다.

- **코스의 `summary3`가 어디에서도 렌더되지 않았다.** `getCourseSummary3()` 함수와
  `ContentSummary`의 `course` 분기까지 있는데 페이지가 부르지 않아 11편의 3줄 요약이
  HTML에 한 줄도 나가지 않았다. 인사이트만 렌더하고 있었다
- **코스 소개 본문이 `stripMarkdown`을 거쳐 단일 문단으로 출력됐다.** 표와 목록, 질문형
  헤딩을 넣어도 화면에 구조가 남지 않는다
- **클래스와 인사이트가 발행하는 FAQPage를 코스만 발행하지 않았다**

`Course.content`에 원본 마크다운을 담고 `description`은 첫 문단으로 좁혔다. 코스 목록
카드가 소개 세 문단을 통째로 물고 오던 것도 함께 고쳐졌다.

### 2. llms.txt의 코스 목록 누락

11개 코스 가운데 6개만 적혀 있었고, 디지털 마케팅 핵심 용어 코스의 개념 수가 11개로
적혀 있었다(실제 16개). 전체 코스와 각 코스가 담은 개념을 채웠다.

### 3. 반말 의문형 헤딩 9개가 FAQPage로 발행되고 있었다

`korean-prose-rules`는 반말 의문형을 어디에도 쓰지 않는다고 정해 두었고 `check-prose.ts`에
해당 규칙도 있었다. 그런데 규칙이 종결 어미를 하나씩 적어 둔 목록이라 `될까?`, `올랐을까?`,
`시작되었나?` 같은 변형을 놓쳤다. 9개가 남아 있었고 전부 H2라서 FAQPage 질문으로 그대로
발행되고 있었다. 헤딩을 해요체로 고치고, 물음표로 끝나면서 해요체가 아닌 헤딩을 전부 잡도록
규칙을 일반화했다. `## GA4란?` 같은 명사형 헤딩은 그대로 통과한다.

### 4. 본문의 설명과 실제 발행이 어긋난 글 1편

`aeo-geo-practical-setting-case`는 "H2가 9개 있고 전부 물음표로 끝나므로 9개 Q&A가
FAQPage로 자동 발행된다"고 적어 두었는데, 실제 H2에 물음표가 없어 한 쌍도 추출되지 않고
있었다. AEO 세팅을 설명하는 글에서 그 세팅이 빠져 있던 셈이다. 질문형 H2 9개에 물음표를
붙여 본문의 설명과 실제를 맞췄다(현재 12쌍 발행).

## 보강 내역

| 조치 | 편수 |
|---|---|
| 자주 묻는 질문 신설 | 63 |
| 내부 링크 추가 | 63 |
| metaDescription 재작성 | 64 + 코스 4 |
| 태그 보강 | 23 |
| metaTitle 교정 | 3 |
| ogDescription 신규 | 2 |
| 코스 소개 본문 재작성 | 11 |
| 반말 의문형 헤딩 교정 | 9 |
| 자주 묻는 질문 헤딩 표기 통일 | 31 |

## 결과

| 구분 | 종합 | SEO | AEO | GEO |
|---|---|---|---|---|
| insights (220) | 86.2 → 92.0 | 94.2 → 96.6 | 75.4 → 86.9 | 87.5 → 91.9 |
| classes (131) | 81.8 → 88.1 | 85.3 → 94.0 | 77.8 → 88.8 | 81.7 → 81.7 |
| courses (11) | 39.5 → 78.4 | 83.6 → 95.9 | 19.4 → 91.8 | 12.7 → 49.3 |
| 전체 (362) | 83.2 → 90.2 | 90.6 → 95.6 | 74.6 → 87.7 | 83.1 → 86.9 |

등급 분포: A 255 → 338편, B 94 → 24편, C 9 → 0편, D 4 → 0편.

`seo-audit.ts` 이슈 수는 32건으로 작업 전후가 같다. 새로 생긴 기술 SEO 문제는 없다.

## 남긴 항목과 이유

| 규칙 | 미달 | 이유 |
|---|---|---|
| R-GEO-01 출처 인용 | FAIL 35, PARTIAL 51 | 외부 출처는 주제별로 실제 근거가 있을 때만 넣는다. 점수를 올리려고 기관명과 보고서를 끼워 넣지 않는다 |
| R-GEO-02 통계 밀도 | FAIL 29 | 같은 이유. 수치는 지어낼 수 없다 |
| R-GEO-04 전문가 신호 | FAIL 51 | 경험 서술은 실제 경험이 있는 주제에만 쓴다 |
| R-GEO-05 콘텐츠 신선도 | FAIL 61 | `updatedAt`을 일괄로 올리면 실제로 고치지 않은 글의 수정일이 오늘이 된다. 내용을 고친 글에만 붙인다 |
| R-AEO-03 질문형 헤딩 비율 | FAIL 44, PARTIAL 206 | 30% 기준은 `korean-prose-rules`의 헤딩 규칙(명사형 원칙, 질문형은 FAQ 섹션 중심)과 정면으로 부딪힌다. 자주 묻는 질문 섹션으로 올릴 수 있는 데까지만 올렸다 |
| R-AEO-07 엔티티 명확성 | FAIL 45 | 약어 판정 오탐이 남아 있다. 판정을 사람이 한 번 거쳐야 하는 항목이다 |
| R-SEO-02 metaDescription | PARTIAL 106 | 100~119자 구간. FAIL 0건이고 실사용에 문제가 없어 이번 범위에서 뺐다 |

## 도구

- `npm run check:geo` — 전수 점수
- `npx tsx scripts/geo-seo-score.ts --slug <slug>` — 한 편 상세
- `npx tsx scripts/geo-seo-score.ts --rule R-GEO-01` — 규칙별 미달 목록
- `npx tsx scripts/geo-seo-score.ts --worklist` — 안전하게 고칠 수 있는 미달 항목만 파일 단위로
- `npx tsx scripts/geo-seo-score.ts --csv out.csv` — 전 편 점수 CSV
